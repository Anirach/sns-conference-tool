# Assets

Place bundled resources here. A real `sample.pdf` is expected for the `file.pickArticle` bridge stub to return a plausible payload; in pass 1 only the path is surfaced — no file is actually read — so a placeholder is acceptable.
